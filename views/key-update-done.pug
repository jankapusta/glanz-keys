extends layout

block content
    div(class='success')  
      p New key name is
        strong(class='keyName') #{officeKey.key_name} 
      p Thank you! 
        a(href='/admin' class='button-secondary pure-button inline') back 